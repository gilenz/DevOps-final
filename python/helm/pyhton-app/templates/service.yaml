apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyhton-app.fullname" . }}
  labels:
    {{- include "pyhton-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyhton-app.selectorLabels" . | nindent 4 }}
