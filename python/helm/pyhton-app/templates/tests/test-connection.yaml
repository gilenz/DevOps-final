apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyhton-app.fullname" . }}-test-connection"
  labels:
    {{- include "pyhton-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pyhton-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
